# GnuChan Engine Proje Amacı

## build
py.exe makefile.py --build --clean -j4


---

### GnuChanIDE Marka & Lisans
`AUTHORS.md`, `CONTRIBUTING.md`, `COPYRIGHT.txt`, `DONORS.md`, `LOGO_LICENSE.md`, `LICENSE.txt`

motorun adini GnuChanIDE olarak degistir ve windows, gnu/linux disindaki platformlari kaldir export template icindende kaldir

calistirilabilir iconu D:\GnuChan_Engine\game_IDE\icon.png ile degistir
help -> about GnuChanIDE icindeki resim hala ayni  -> D:\GnuChan_Engine\game_IDE\icon.png ile degistir
tema sisteminin arka plan rengi-> 1c0532 ve 4d1088
bootsplash daki resim pencereye gore gerilmesi gerekiyor


bullet, visual_script, fbx kaldirilicak
AssetLib tamamen kaldirilicak


motorun web versiyonu, mac versiyonu falan silinicek sadece gnu/linux ve windows versiyonu kalicak
export sadece -> gnu/linux ve windows odakli

export template manager icindeki download and install yeri silinicek dosyadan manuel only olucak
install android build template olayi topdan silinicek
projedeki versiyon kontrol olayi ileride build in sistem olucak

mono topdan silinicek

# oncelikle opengl 4.3 verisoyunu gelistir 4.3 tamamen sorunsuz calistiginda eski opengl versiyonlari kaldirilicak

---

# PARTLI İŞ PLANI (12 Part)

Her part sonunda build test yapılacak: `py.exe -m SCons -C d:\GnuChan_Engine\game_IDE platform=windows tools=yes target=release_debug -j4`

---

## [x] PART 1: Build Sistemi & Platform Temizliği ✅
- [x] `platform/android/` silindi
- [x] `platform/iphone/` silindi
- [x] `platform/osx/` silindi
- [x] `platform/javascript/` silindi
- [x] `platform/server/` silindi
- [x] `platform/uwp/` silindi
- [x] `SConstruct` platform alias'ları temizlendi (macos, ios, web kaldırıldı)
- [x] `scons: done building targets.` ✅ Build başarılı

---

## [x] PART 2: Marka & İsim Değişikliği - GnuChanIDE ✅
- [x] `version.py` zaten güncel (gcIDE / GnuChanIDE)
- [x] `core/version_generated.gen.h` template güncel
- [x] `main/main.cpp` "Godot Engine" → "GnuChan Engine", "GnuChan Engine contributors"
- [x] `main/splash.png` bootsplash güncellendi (= icon.png)
- [x] `main/app_icon.png` güncellendi (= icon.png)
- [x] `main/main.cpp` `application/config/icon` default → `res://icon.png`
- [x] `editor/editor_themes.cpp` `create_unscaled_default_project_icon()` artık `app_icon.png` döndürüyor → yeni projelerde res://icon.png GnuChanIDE ikonu
- [x] Platform dosyalarında godot → gnuchanide (windows/SCsub, methods.py)
- [x] Build test ✅

---

## [x] PART 3: Exe İkonu & About Diyaloğu ✅
- [x] `platform/windows/godot.ico` → `game_IDE/icon.png` kullanılarak yeniden oluşturuldu (16-256px çoklu boyut)
- [x] `platform/windows/godot_res.rc` güncellendi (LegalCopyright, Info URL)
- [x] `editor/editor_about.cpp` logosu Godot SVG yerine `app_icon.png`'den yükleniyor
- [x] `editor/editor_node.cpp` "About Godot" → "About", "Support Godot" → "Support GnuChan"
- [x] Build test + editor açılış ✅

---

## [x] PART 4: Tema & Bootsplash ✅
- [x] `editor/editor_themes.cpp` Default preset arka plan rengi → #1c0532 (base) / #4d1088 (accent)
- [x] `main/splash.png` güncellendi (= icon.png)
- [x] `main/main.cpp` bootsplash stretch/scale ayarı (boot_logo_scale ile pencereye göre gerilme)
- [x] Build test ✅

---

## [x] PART 5: Module Temizliği - bullet, visual_script, fbx ✅
- [x] `modules/bullet/` silindi
- [x] `modules/visual_script/` silindi
- [x] `modules/fbx/` silindi
- [x] `thirdparty/bullet/` silindi
- [x] `SConstruct` `builtin_bullet` opsiyonu kaldırıldı
- [x] `scons: done building targets.` ✅ Build başarılı

---

## [x] PART 6: AssetLib & Mono Temizliği ✅
- [x] `modules/mono/` — zaten silinmişti
- [x] `editor/plugins/asset_library_editor_plugin.*` — zaten silinmişti
- [x] `editor_node.cpp` AssetLib shortcut'ları temizlendi (ALT+4 ve CTRL+F4)
- [x] `editor_node.h` `EDITOR_ASSETLIB` — zaten temizdi
- [x] `modules_enabled.gen.h` — `MODULE_BULLET_ENABLED`, `MODULE_FBX_ENABLED`, `MODULE_VISUAL_SCRIPT_ENABLED` kaldırıldı
- [ ] Build test (yapılacak)

---

## [x] PART 7: Export Sistemi & Template Manager ✅

### export_template_manager
- [x] Tüm download/mirror/internet kodu kaldırıldı (HTTPRequest, mirror listesi, progress bar, cancel)
- [x] Sadece "Install from File" (.tpz) ve "Uninstall" kaldı
- [x] Header temizlendi — mirror, download, android ile ilgili tüm member'lar silindi

### editor_export / EditorExportPlugin
- [x] iOS ile ilgili tüm kod kaldırıldı (add_ios_framework, add_ios_plist_content, add_ios_cpp_code, etc.)
- [x] macOS ile ilgili tüm kod kaldırıldı (add_osx_plugin_file, get_osx_plugin_files)
- [x] `_bind_methods()` iOS/macOS bind'ları kaldırıldı
- [x] `_clear()` / `_export_end()` içindeki iOS/macOS cleanup kaldırıldı

### editor_node
- [x] Android build template ile ilgili tüm kod kaldırıldı:
  - `FILE_INSTALL_ANDROID_SOURCE`, `FILE_EXPLORE_ANDROID_BUILD_TEMPLATES`, `SETTINGS_INSTALL_ANDROID_BUILD_TEMPLATE` enum'ları kaldırıldı
  - `_android_build_source_selected()` fonksiyonu kaldırıldı
  - `custom_build_manage_templates`, `install_android_build_template`, `remove_android_build_template` dialog'ları kaldırıldı
  - `file_android_build_source` dialog'u kaldırıldı
  - `#ifdef ANDROID_ENABLED` preprocessor blokları kaldırıldı
  - Menü item'ları ve tooltip'leri kaldırıldı
- [ ] Build test (yapılacak)

---

## [ ] PART 8: Versiyon Kontrol & Driver Temizliği
- [ ] `editor/plugins/version_control_editor_plugin.*` sil
- [ ] Editor VCS referanslarını temizle
- [ ] Core VCS sınıflarını temizle
- [ ] `drivers/` gereksiz driver'ları temizle
- [ ] Build test

---

## [ ] PART 9: OpenGL 4.3 Başlangıç
- [ ] `drivers/gles3/` → `drivers/gles4/` kopyala
- [ ] GL 4.3 core profile shader'ları
- [ ] GL 4.3 context oluşturma
- [ ] `gles_builders.py` güncelle
- [ ] `SConstruct` GLES4 builder ekle
- [ ] Build test

---

## [ ] PART 10: GLES4 Renderer Geliştirme
- [ ] Temel render pipeline
- [ ] Vertex buffer, uniform buffer, shader program
- [ ] Texture, material, mesh rendering
- [ ] Canvas (2D) rendering
- [ ] GL 4.3 wrapper/fonksiyon yükleme
- [ ] Build test + editor açılış

---

## [ ] PART 11: GLES2 & Eski GL Sürümlerini Kaldırma
**NOT:** Sadece GLES4 (Part 10) tamamen çalışır durumda olduğunda yapılacak!
- [ ] `drivers/gles2/` sil
- [ ] GLES2 builder'ları kaldır
- [ ] Platform GLES2 tanımlarını kaldır
- [ ] Build test

---

## [ ] PART 12: Son Temizlik & Dokümantasyon
- [ ] Lisans dosyalarını güncelle
- [ ] `README.md` güncelle
- [ ] Final `godot` string search
- [ ] `thirdparty/` temizle
- [ ] Final clean build test

---

# BUG FIXES

## [x] Yeni Proje Boş Oluşuyor — `project.gnuchan` kaydedilemiyor ✅
- **Tarih:** 2026-07-29
- **Dosya:** `core/project_settings.cpp`
- **Kök neden:** `save_custom()` fonksiyonunda `.gnuchan` uzantısı tanınmıyordu — sadece `.godot` ve `override.cfg` kontrol ediliyordu. `ProjectManager::ok_pressed()` içinde `initial_settings` ile `save_custom()` çağrılınca "Unknown config file format" hatası alınıyor, `project.gnuchan` oluşturulamıyor, proje boş kalıyordu.
- **Fix:** `save_custom()` fonksiyonundaki format kontrolüne `.gnuchan` eklendi:
  ```cpp
  if (p_path.ends_with(".gnuchan") || p_path.ends_with(".godot") || p_path.ends_with("override.cfg"))
  ```
- Build test: yapılacak

---

**ÖNEMLİ KURALLAR:**
1. Her part TEK seferde yapılır, build test edilir, sonra bir sonraki part'a geçilir
2. Part 11 (GLES2 silme) sadece Part 10 tamamen çalışır durumda olduğunda yapılır
3. Her part öncesi ve sonrası git commit atılır
4. Build hatası alınırsa bir sonraki part'a geçilmez, hata çözülür
