# modular programming

#extern NODE_NAME

# no need for basic language grammar for now

# for now

void _ready() {

}

void _update(float delta) {

}

void _update_physics(float delta) {

}

# Holder amaci node type demeden sadece kisa sureligine tutmasi
Node.Childs -> return child list
Node.AddChild()  -> NodeRefence ile de gonderilebilir
Node.DropChild() -> NodeRefence ile de drop yapilabilir eger drop olursa sahnenin root objesi parent oluyor

Node.delete;
Node.hide; -> sadece gizlemek
Node.Show; -> gorunur yapmak
Node.Disable; -> alt collision herseyi devre disi birakmak
Node.Enable;  -> tekrar herseyi devreye sokmak

# raycast 3d node
# return yapan seyler icin parantez gereksiz

Holder Node = $"raycast".GetNode();

$"raycast".GetName;
$"raycast".GetGroupName;
$"raycast".Length = 10;
$"raycast".Skip(body_name);
$"raycast".SkipList(body_name, body_name, body_name, body_name, body_name);
$"raycast".SkipGrupName("Grup_Name");
$"raycast".getNode;


Node.Parent = Node.Give; -> parent degistirme
# parent degistiginde yada parent olundugunda butun child transform parent ile nodu ile esitleniyor


if ($"raycast".GetName == "kup1") {
    printf("working now \n");
} #// --> elif, else if, else

NodeReference Hand = NULL;

if (Hand == NULL) {
    $"node".Parent = $"raycast".Take;
    Hand = $"raycast".take;
} else {
    $"node".Drop(Hand);
    Hand = NULL
}